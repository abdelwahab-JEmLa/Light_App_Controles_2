# Contexte d'Isolation du TODO

**Fichier Principal :** `C:\Users\Abou Mohamed\AndroidStudioProjects\Light_App_Controles\app\src\main\java\EntreApps\Shared\Ui\Dialog\A_PressistatntMainActivityButtons_App4.kt`
**Cible :** `//TODO(1): affiche ici le nom de client par ad activeOnVent_M2Client et fait pour les autres infos de t_copiePattersApp`

## 1. Flux d'exécution attendu (Flow)
Le but est d'afficher le nom du client actif (`activeDatas.activeOnVent_M2Client`) sous ou à côté de la section "Bon Vent" dans la boîte de dialogue `PressistatntMainActivityButtons_App4`.
- On récupère l'instance `M2Client` via `activeDatas.activeOnVent_M2Client`.
- Si celle-ci n'est pas nulle, on affiche son nom en utilisant la méthode `client.getNomAffichage()` ou `client.nom` (par exemple, dans un composable `Text` avec un style approprié).
- Puisque ce TODO fait référence à `t_copiePattersApp`, il faut s'inspirer du projet `ClientJetPack` pour formater l'affichage (nom, numéro, temps écoulé, total...).

## 2. Fichiers Relatifs Impliqués (Projet Local Light_App_Controles)
- `C:\Users\Abou Mohamed\AndroidStudioProjects\Light_App_Controles\app\src\main\java\EntreApps\Shared\Models\Relative_Vents\Models\M2Client.kt`
- `C:\Users\Abou Mohamed\AndroidStudioProjects\Light_App_Controles\app\src\main\java\com\example\light_app_controles\B\Screens\Z\Screens\Test\ID1\Client_Map\App\Bon_Vent_Etate\View\a\Screens\a\Vendeur_Boutiqe\Screen\ViewModel\ActiveDatasFragNewProto.kt`
- `C:\Users\Abou Mohamed\AndroidStudioProjects\Light_App_Controles\app\src\main\java\EntreApps\Shared\Models\Relative_Vents\Models\M8BonVent.kt`

## 3. Fichier Relatif (Projet Source ClientJetPack - Pattern à copier)
- `D:\AndroidStudioProjects\ClientJetPack\app\src\main\java\P0_MainScreen\Main\Main\Settings\FWinID1\AbdelwahabEBoutiquePressistantsOverAll\Windows\But4\ClientSearch\Option\Z\ClientSearchItem\View\ID4ClientSearchButton.kt` : Contient le pattern visuel d'affichage du nom du client (référence au TODO 2.C).

## 4. Code environnant (Highlighted Context - Local)
```kotlin
            HorizontalDivider()
            Text("Bon Vent")         //<--
            // ---> TODO: //TODO(1): affiche ici le nom de client par ad activeOnVent_M2Client et fait pour les autres infos de t_copiePattersApp <---
            Row {
                activeOnVent_M8BonVent_benefice?.let { benef ->
```

## 5. Extraits des modèles locaux
### Extrait de `M2Client.kt` :
```kotlin
    fun getNomAffichage(): String {
        return nomPrenomArabe.takeIf { it.isNotBlank() } ?: nom
    }
```

### Extrait de `ActiveDatasFragNewProto.kt` :
```kotlin
    val activeOnVent_M2Client: M2Client? by derivedStateOf {
        activeOnVent_M8BonVent?.parent_M2Client_KeyID
            ?.let { clientKey -> list_M2Client?.find { it.keyID == clientKey } }
    }
```

## 6. PATTERN À COPIER DEPUIS ClientJetPack (ID4ClientSearchButton.kt)
Voici comment l'affichage est géré dans le projet source (Lignes 161-199) :
```kotlin
            if (showLabels) {
                val nomClient = getter.activeOnVent_M2Client?.nom ?: ""
                val onVentId8BonVent = getter.activeOnVent_M8BonVent

                val phoneDisplay = formatPhoneDisplay(
                    numTelephone = getter.activeOnVent_M2Client?.numTelephone ?: "",
                    nomClient = nomClient
                )

                Text(            //<--
                //TODO(2.C Relative Au Todo(1): 
                        //... depuit ici 
                    text = if (isTextCollapsed) {
                        // Collapsed: just name + phone hint
                        "$nomClient$phoneDisplay"
                    } else {
                        onVentId8BonVent?.let { bon ->
                            val timeElapsed = getTimeElapsedString(bon.creationTimestamps)
                            val (totalProducts, totalValue) = get_vents_datas(aCentralFacade,list_m13_uiState_viewModelNewProtoPatterns)

                            if (bon.parent_M2Client_DebugInfos.isNotEmpty() &&
                                bon.parent_M2Client_DebugInfos != "Non Defini"
                            ) {
                                // Full label: Name 📞O23 - 2h 5m - 3 P - 1500.00 DA
                                "$nomClient$phoneDisplay - $timeElapsed - $totalProducts P - ${
                                    String.format("%.2f", totalValue)
                                } DA"
                            } else "Rechercher Client"
                        } ?: run {
                            val count = clientsWithCommandBonVents.size
                            if (count > 0) "$count clients ouverts" else "Aucun client ouvert"
                        }
                    },
                    modifier = Modifier
                        .background(Color(0xFF4CAF50))
                        .padding(4.dp)
                        .clickable { isTextCollapsed = !isTextCollapsed },
                    color = Color.White
                )
            }
```
L'assistant qui va régler ce `TODO(1)` devra s'inspirer de la logique `isTextCollapsed` / `timeElapsed` / `totalProducts` / `totalValue` pour mettre à jour la boîte de dialogue dans le projet local.

## 7. Règles d'Isolation (Ignore Rules)
```text
*
!*/
!*skill_agent/**
!*A_PressistatntMainActivityButtons_App4.kt
!*M2Client.kt
!*ActiveDatasFragNewProto.kt
!*M8BonVent.kt
!*ID4ClientSearchButton.kt
```
